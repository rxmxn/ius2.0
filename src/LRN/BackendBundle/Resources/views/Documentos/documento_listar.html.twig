{% extends "BackendBundle::backend.html.twig" %}

{% block menu %}
    <li class="">
        <a href="{{ path('backend_conceptos_listar') }}">Conceptos</a>
    </li>
    <li class="">
        <a href="{{ path('backend_estadisticas_listar') }}">Estadisticas</a>
    </li>
    <li class="">
        <a href="{{ path('backend_tutorial_listar') }}">Tutoriales</a>
    </li>
    <li class="active">
        <a href="{{ path('backend_documento_listar') }}">Documentos</a>
    </li>
    <li>
        <a href="{{ path('backend_homepage') }}">Noticias</a>
    </li>
    <li class="">
        <a href="{{ path('backend_videos_listar') }}">Videos</a>
    </li>
{% endblock %}

    {% block contenido %}

        <div class="container" style="margin-bottom: 20px;">

            <div class="clearfix" style="height: 100%;width: 95%">
                <div class="span2">

                    <h1>Documentos</h1>

                    <table class="table table-striped">
                        <tr>
                            <th> </th>
                            <th>Tema Relacionado</th>
                            <th>Nombre Documento</th>
                            <th>Documento</th>
                            <th>Acci&oacute;n</th>
                        </tr>

                        {% for documento in documentos %}
                            <tr>
                                <td>{{ loop.index }}</td>
                                <td>{{ documento.temaRelacionado }}</td>
                                <td>{{ documento }}</td>
                                <td><a href="{{ asset('uploads/pdfs/' ~ documento.rutaPDF) }}">{{ documento.rutaPDF }}</a></td>
                                <td>
                                    <a href="{{ path('backend_documento_editar', { 'id': documento.id }) }}" class="btn btn-primary">Editar</a>
                                    <a href="{{ path('backend_documento_borrar', { 'id': documento.id }) }}" class="btn btn-danger">Borrar</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </table>

                    <a href="{{ path('backend_documento_crear') }}" class="btn btn-default">Añadir Documento</a>
                </div>
            </div>
        </div>

    {% endblock %}
