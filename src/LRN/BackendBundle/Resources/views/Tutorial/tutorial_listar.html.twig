{% extends "BackendBundle::backend.html.twig" %}

{% block menu %}
    <li class="">
        <a href="{{ path('backend_conceptos_listar') }}">Conceptos</a>
    </li>
    <li class="">
        <a href="{{ path('backend_estadisticas_listar') }}">Estadisticas</a>
    </li>
    <li class="active">
        <a href="{{ path('backend_tutorial_listar') }}">Tutoriales</a>
    </li>
    <li class="">
        <a href="{{ path('backend_documento_listar') }}">Documentos</a>
    </li>
    <li class="">
        <a href="{{ path('backend_homepage') }}">Noticias</a>
    </li>
    <li class="">
        <a href="{{ path('backend_videos_listar') }}">Videos</a>
    </li>
{% endblock %}

    {% block contenido %}

        <div class="container" style="margin-bottom: 20px;">

            <div class="clearfix" style="height: 100%;width: 95%">
                <div class="span12">

                    <h1>Redes Sociales</h1>

                    <table class="table table-striped">
                        <tr>
                            <th> </th>
                            <th>Red Social</th>
                            <th>Fecha Publicaci&oacute;n</th>
                            <th>Descripci&oacute;n</th>
                            <th>Acci&oacute;n</th>
                        </tr>

                        {% for tutorial in tutoriales %}
                            <tr>
                                <td>{{ loop.index }}</td>
                                <td><a href="{{ path('backend_tutorial_mostrar', { 'id': tutorial.id }) }}">
                                    {{ tutorial }}
                                    </a>
                                </td>
                                <td>{{ tutorial.fechaPublicacion|date('d/m/Y') }}</td>
                                <td>{{ tutorial.descripcion }}</td>
                                <td>
                                    <a href="{{ path('backend_paso_crear', { 'tutorial_id': tutorial.id }) }}" class="btn btn-warning">Agregar Paso</a>
                                    <a href="{{ path('backend_tutorial_editar', { 'id': tutorial.id }) }}" class="btn btn-primary">Editar</a>
                                    <a href="{{ path('backend_tutorial_borrar', { 'id': tutorial.id }) }}" class="btn btn-danger">Borrar</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </table>

                    <a href="{{ path('backend_tutorial_registro') }}" class="btn btn-default">Añadir Tutorial</a>
                </div>
            </div>
        </div>

    {% endblock %}
