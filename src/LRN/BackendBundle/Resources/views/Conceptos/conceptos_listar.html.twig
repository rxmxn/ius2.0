{% extends "BackendBundle::backend.html.twig" %}

{% block menu %}
    <li class="active">
        <a href="{{ path('backend_conceptos_listar') }}">Conceptos</a>
    </li>
    <li class="">
        <a href="{{ path('backend_estadisticas_listar') }}">Estadisticas</a>
    </li>
    <li class="">
        <a href="{{ path('backend_tutorial_listar') }}">Tutoriales</a>
    </li>
    <li class="">
        <a href="{{ path('backend_documento_listar') }}">Documentos</a>
    </li>
    <li>
        <a href="{{ path('backend_homepage') }}">Noticias</a>
    </li>
    <li class="">
        <a href="{{ path('backend_videos_listar') }}">Videos</a>
    </li>
{% endblock %}

    {% block contenido %}

        <div class="container" style="margin-bottom: 20px;">

            <div class="clearfix" style="height: 100%;width: 95%">
                <div class="span12">

                    <h1>Conceptos</h1>

                    <table class="table table-striped">
                        <tr>
                            <th> </th>
                            <th>Subt&iacute;tulo</th>
                            <th>Texto</th>
                            <th>Acci&oacute;n</th>
                        </tr>

                        {% for concepto in conceptos %}
                            <tr>
                                <td>{{ loop.index }}</td>
                                <td><a href="{{ path('backend_conceptos_mostrar', { 'id': concepto.id }) }}">
                                        {{ concepto }}
                                    </a>
                                </td>
                                <td>{{ concepto.texto }}</td>
                                <td>
                                    <a href="{{ path('backend_conceptos_editar', { 'id': concepto.id }) }}" class="btn btn-primary">Editar</a>
                                    <a href="{{ path('backend_conceptos_borrar', { 'id': concepto.id }) }}" class="btn btn-danger">Borrar</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </table>

                    <a href="{{ path('backend_conceptos_crear') }}" class="btn btn-default">Añadir Concepto</a>
                </div>
            </div>
        </div>

    {% endblock %}
